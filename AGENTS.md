# AGENTS.md

## Git Conventions
- This repository follows Conventional Commits.
- Use commit messages in the format `type(scope): short summary` (for example: `fix(workflow): fetch full git history and tags`).
- Keep commits focused to a single concern and avoid bundling unrelated changes.
